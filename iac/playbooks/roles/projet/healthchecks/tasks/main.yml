---
- name: Check Project health
  uri:
    url: http://192.168.56.64:3000/health
    return_content: yes
  # Register the output of the module in a variable
  register: project_health

- name: Print Project health
  debug:
    msg: "{{ project_health.content }}"

